const initialState = {
  supply: [],
  players: [],
  currentPlayer: null
};

const game = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default game;
